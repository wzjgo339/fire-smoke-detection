# D-Fire Dataset configuration for YOLO
# `path` is relative to the current working directory; run scripts from the project root.
path: .

train: data/train.txt
val: data/val.txt
test: test/images

nc: 2
names:
  0: smoke
  1: fire
